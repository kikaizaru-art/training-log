---
type: design-memo
area: training-log
status: design-only
created: 2026-05-05
updated: 2026-05-05
tags: [training-log, design-memo, dashboard, bases, artifact, quick-mode]
related: [[_index]], [[../../../90-Meta/lint-runbook]]
sources:
  - "[[../../career/2026-05-04_dispatch-session-summary]]"
  - "[[training-sessions.base]]"
  - "[[training-exercises.base]]"
  - "[[../prompts/training-log-session]]"
project: training-log-enhancement
---

# 筋トレMEMO 拡張機能 設計メモ

dispatch session #4（2026-05-05）で議論した3機能の **設計のみ** 記録。実装は別セッションで walking through する想定。

## 設計対象3機能

### 1. 部位別 last_trained ビュー（Bases 軽実装）

**目的**: 「最後にいつ胸を鍛えたか」「肩を3週間以上鍛えていない」等を一目で把握。

**実装方針**:

- 種目マスタ（`exercises/<種目名>.md`）の frontmatter に **`body_part:` field を追加**（例: `chest` / `back` / `shoulders` / `legs` / `arms` / `core` / `compound`）
- `training-exercises.base` に新規ビュー「部位別 last_trained」追加
  - groupBy: `body_part`
  - 集約: `pr_date` の MAX or 各種目の最終実施日（後者が望ましいが、Bases では別計算が必要）
- 既存種目マスタへの body_part 一括補完作業も同時に必要（種目数次第、5-15分）

**Bases の限界**: 「種目マスタの最終実施日」は **セッション側に紐づく情報** なので、Bases formula で取れない可能性。session の種目フィールドから逆引きが必要。実装時に検証。

**フォールバック**: 上記が困難なら、種目マスタの `last_trained:` field を Claude が筋トレ報告時に**自動更新**する運用にする（prompts/training-log-session.md 改修）。

### 2. 4軸サマリパネル（artifact 重実装）

**目的**: morning-dashboard を朝開いた時に、過去N週間の筋トレ実施状況を4軸でビジュアル把握。

**4軸の定義**（推奨案）:

- 時間（duration_min）
- ボリューム（total_volume_kg）
- 体調（condition）
- 気分（mood）

**実装方針**:

- 既存 `morning-dashboard` artifact に新規パネル「💪 今週のトレーニング」追加
- callMcpTool で Vault の training-sessions/ 配下を読み取り（実装時に Read tool 経由でファイル列挙する仕組みを設計）
- Chart.js で4軸を**重ね合わせ折れ線グラフ** or **小さい4枚のスパークライン**として描画
  - 重ね合わせは縦軸スケール統一が困難 → スパークライン4枚案を推奨
- 期間は直近4週間 + 月次推移2案を切替可能

**実装の重さ**:

- 既存 artifact (1000行程度想定) の構造把握 + パネル追加で 300-500行追加
- callMcpTool で Vault 読み取りの仕組み設計が要
- データサンプル不足の段階で UI 設計しても机上の空論になりがちなので、**Phase 0 で2-3週間ログを貯めてから着手**する方が現実的

### 3. quick 即確定モード（実装済確認）

**目的**: 「いつもどおり」の一言で前回と同じ種目・重量・レップで即確定。

**現状**: `20-Areas/health/prompts/training-log-session.md` に「いつもどおり即確定パース表」が**既に実装済**（2026-05-04 16:04 ログより確認）。

**追加検討項目**（実装時に検証）:

- 「いつもどおり、最後だけ重量+2.5kg」のような部分指定パターンの拡張
- 「先週と同じ」「3回前と同じ」等の参照ベース指定
- 「PR チャレンジ」モード（メイン種目だけ重量+5kgで挑戦の意思表明 → 失敗時のリカバリ手順案内）

## 過去分ログの取り込み

- 既存 `prompts/training-log-session.md` は **過去日付指定での入力に対応**（「日付推測NG」のガード節がある分、明示的に過去日を指定すれば入力可能）
- 過去ログの遡及入力は Phase 0 期間中に随時実施可能。本設計メモは過去ログ入力機能の追加実装は不要と判断

## 実装順序（推奨）

1. **種目マスタへの `body_part` field 追加運用ルール化**（軽、5分）
2. **Bases 部位別 last_trained ビュー追加**（軽-中、15-30分、Bases formula 検証込み）
3. **既存種目マスタへの body_part 一括補完**（種目数次第、5-15分）
4. **2-3週間 Phase 0 ログ蓄積**（待ち期間）
5. **morning-dashboard 4軸パネル追加**（重、1-2時間、データサンプル確認後）
6. **quick 即確定モード拡張**（中、30分、必要性が出たタイミングで）

## Phase 0 中の運用方針

- 過去ログの遡及入力は随時実施可能（既存プロンプトで対応）
- 上記実装順序の 1-3 は dispatch 後の別セッションで早めに着手
- 4-6 は Phase 0 期間中の感覚値蓄積後に再評価

## オーナー判断（2026-05-05 dispatch）

- dispatch 内では設計メモのみ
- 実装は別セッションで walking through
- 過去分ログは既存プロンプトで入力できる前提（追加実装不要）

## 関連ノート

- [[_index]] - training-log エリアトップ
- [[training-sessions.base]] - セッション記録 Bases
- [[training-exercises.base]] - 種目マスタ Bases（部位別 last_trained 追加先）
- [[../prompts/training-log-session]] - quick 即確定モード実装先
- [[../../career/2026-05-04_dispatch-session-summary]] - 元 dispatch 経緯
