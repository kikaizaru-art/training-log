---
type: dev-plan
area: training-log
status: in-progress
created: 2026-06-17
updated: 2026-06-17
tags: [training-log, dev-plan, form-input, meta]
related: [[_index]], [[2026-05-05_拡張機能-設計メモ]]
---

# 開発プラン: 筋トレログの「筋トレMEMO風」アプリ化（Obsidian強化版）

## 確定方針

- **形態**: 別アプリは作らず、既存 Obsidian 構成を強化して「アプリらしさ」を実現
- **SSoT**: 現リポジトリの Markdown（YAML frontmatter）を維持。フォームは入力 UI に徹する
- **入力 UI**: **日本語フォーム入力式**（Meta Bind ＋ Templater による frontmatter 直書き＋プリフィル）
- スキーマ判断: `status` は `completed/partial/skipped` に統一 / `condition`・`mood` は任意 / `updated` を正式化 / `body_part` は6値の正規タクソノミ

## 現状診断（着手前の技術的負債）

| # | 問題 | 状態 |
|---|------|------|
| 1 | Bases の `inFolder` が旧 Vault パス（`20-Areas/health/training-log/...`）で全ビュー0件化リスク | ✅ Phase 1 で修正 |
| 2 | スキーマ・ドリフト（`status: complete` 9件 / `condition`・`mood` 欠落 / `updated` 未定義） | ✅ Phase 1 で正規化 |
| 3 | `body_part` 表記ゆれで部位別グルーピング不能 | ✅ Phase 1 で統一 |
| 4 | セットデータが本文テキストで未構造化（種目別グラフ自動化の障壁） | Phase 4 で判断 |

## フェーズ

### Phase 1: データ基盤の正規化 ✅ 完了（2026-06-17）

- `.base` ×2 の `inFolder` を独立リポジトリ構成（`sessions` / `exercises`）に修正
- 全セッションの `status: complete` → `completed` に正規化（9件）
- `body_part` を正規6値（`胸/背中/肩/脚/腕/体幹`）に統一、詳細筋を `body_part_detail` へ退避（種目9件＋テンプレ）
- README スキーマ SSoT を実態に更新（`condition`/`mood` 任意化、`updated` 正式化、`body_part_detail`/`bodyweight` 追加、正規タクソノミ表）
- 種目 Base に `body_part_detail` 表示名追加

### Phase 2: 入力体験のフォーム化 ✅ 初版完了（2026-06-17）

- `templates/_フォーム入力ガイド.md`: 必要プラグイン（Meta Bind / Templater / QuickAdd）と使い方
- `templates/session-<routine>.md` ×3: 前回値プリフィル済み・Meta Bind フォーム欄付きテンプレ
- `_template-session.md`: フォーム欄＋新スキーマに更新
- README 運用フローに「フォーム入力 / 対話入力」の2経路を明記
- 残: quick 即確定モード拡張（[[2026-05-05_拡張機能-設計メモ]] 機能3）、QuickAdd ボタンの実機セットアップ

### Phase 3: ダッシュボード強化（未着手）

- 部位別 last_trained ビュー（設計メモ機能1）。`body_part` 統一により前提は整った
- `_index` のホーム画面化（今日のメニュー＋直近完了率＋PR直近＋未刺激部位）
- 4軸サマリ（ボリューム・体調・気分・頻度）の軽量版

### Phase 4: グラフ・PR追跡（未着手 / データ蓄積後）

- セット単位データ構造化の意思決定（本文テキスト維持 vs Dataview インラインフィールド）
- Obsidian Charts / Bases チャートで種目別重量推移・総ボリューム推移を描画
- ブレイクスルー検知（[[progression-rules]] 連携）と PR 履歴自動追跡

### Phase 5: 運用・自動化（未着手）

- スキーマ lint（SessionStart hook 化も検討）
- セッション保存 → `exercises/*.md` の `pr_*` 更新フロー明文化
- git 運用（追記 → commit → push）の定常化

## 実行順序

```
Phase 1 ✅ → Phase 2 ✅(初版) → Phase 3 / Phase 4(データ後) → Phase 5
```

## 関連ノート

- [[2026-05-05_拡張機能-設計メモ]] - 元の3機能設計（本プランで実装フェーズ化）
- [[templates/_フォーム入力ガイド]] - フォーム入力の導入手順
- [[README]] - スキーマ・命名規則 SSoT
- [[_index]] - training-log トップ
