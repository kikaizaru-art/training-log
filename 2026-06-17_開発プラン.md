---
type: dev-plan
area: training-log
status: in-progress
created: 2026-06-17
updated: 2026-06-17
tags: [training-log, dev-plan, form-input, meta]
related: [[_index]], [[2026-05-05_拡張機能-設計メモ]]
---

# 開発プラン: 筋トレログの「筋トレMEMO風」アプリ化（Obsidian強化版）

## 確定方針

- **形態**: **HTML ダッシュボード**（単一HTML・外部CDN非依存・iPhone/GitHub Pages 対応）。Obsidian プラグインには依存しない
- **SSoT**: 現リポジトリの Markdown（YAML frontmatter）を維持。`build.mjs` が Markdown → `data.json` に変換し、ダッシュボードが読み取る
- **入力 UI**: ダッシュボードの「記録する」タブで日本語フォーム入力 → セッション Markdown を生成して `sessions/` に還元（バックエンド不要）
- スキーマ判断: `status` は `completed/partial/skipped` に統一 / `condition`・`mood` は任意 / `updated` を正式化 / `body_part` は6値の正規タクソノミ

> 経緯: 当初は Obsidian（Meta Bind フォーム）案で着手したが、2026-06-17 に「Obsidian ではなく HTML ダッシュボードにしたい」との方針変更。Phase 1 のデータ正規化はフロントエンド非依存のためそのまま活用し、入力・表示を HTML へ移行した。Meta Bind 用 `templates/` は撤去。

## 現状診断（着手前の技術的負債）

| # | 問題 | 状態 |
|---|------|------|
| 1 | Bases の `inFolder` が旧 Vault パス（`20-Areas/health/training-log/...`）で全ビュー0件化リスク | ✅ Phase 1 で修正 |
| 2 | スキーマ・ドリフト（`status: complete` 9件 / `condition`・`mood` 欠落 / `updated` 未定義） | ✅ Phase 1 で正規化 |
| 3 | `body_part` 表記ゆれで部位別グルーピング不能 | ✅ Phase 1 で統一 |
| 4 | セットデータが本文テキストで未構造化（種目別グラフ自動化の障壁） | ✅ `build.mjs` が本文セット行をパースし解決（種目別グラフ実現） |

## フェーズ

### Phase 1: データ基盤の正規化 ✅ 完了（2026-06-17）

- `.base` ×2 の `inFolder` を独立リポジトリ構成（`sessions` / `exercises`）に修正
- 全セッションの `status: complete` → `completed` に正規化（9件）
- `body_part` を正規6値（`胸/背中/肩/脚/腕/体幹`）に統一、詳細筋を `body_part_detail` へ退避（種目9件＋テンプレ）
- README スキーマ SSoT を実態に更新（`condition`/`mood` 任意化、`updated` 正式化、`body_part_detail`/`bodyweight` 追加、正規タクソノミ表）
- 種目 Base に `body_part_detail` 表示名追加

### Phase 2: HTML ダッシュボード＋フォーム入力 ✅ 初版完了（2026-06-17）

- `build.mjs`: Markdown → `dashboard/data.json` 変換（Node 標準のみ・依存ゼロ）。frontmatter ＋ 本文のセット行（自重/加重・表記ゆれ正規化）をパース
- `dashboard/index.html`: 単一 HTML ダッシュボード（SVGチャート内蔵）。4タブ構成
  - ダッシュボード: サマリカード / 総ボリューム推移 / 部位別 最終実施日
  - 種目別: 部位別 PR 一覧 / 種目別 重量・レップ推移グラフ
  - 履歴: セッション一覧＋セット詳細展開
  - 記録する: routine 選択 → 前回値プリフィル → 数値入力 → セッション Markdown 生成（コピー/DL）
- `.github/workflows/pages.yml`: main push 時に build → GitHub Pages 自動公開
- `dashboard/README.md`: 構成・ローカル閲覧・Pages 公開手順
- `_template-session.md` / README をダッシュボード方向に更新、Meta Bind 用 `templates/` 撤去
- 残: quick 即確定モード（[[2026-05-05_拡張機能-設計メモ]] 機能3）、Pages 有効化の実機設定

### Phase 3: ダッシュボード拡張（一部 Phase 2 で先行実装）

- ✅ 部位別 最終実施日（設計メモ機能1）/ 総ボリューム推移 / 完了率カード は実装済み
- 未: routine 別ボリューム切替、体調・気分の推移、今日の推奨メニュー（[[daily-schedule]] 連携）表示

### Phase 4: グラフ・PR追跡の高度化（一部 Phase 2 で先行実装）

- ✅ 種目別 重量/レップ推移グラフ（本文セット行のパースで実現。Dataview 不要だった）
- 未: ブレイクスルー検知（[[progression-rules]] 連携）、PR 更新の自動ハイライト、セット内訳の可変入力対応

### Phase 5: 運用・自動化（未着手）

- スキーマ lint（SessionStart hook 化も検討）
- セッション保存 → `exercises/*.md` の `pr_*` 更新フロー明文化
- git 運用（追記 → commit → push）の定常化

## 実行順序

```
Phase 1 ✅ → Phase 2 ✅(HTMLダッシュボード初版) → Phase 3/4 残タスク → Phase 5
```

## 関連ノート

- [[2026-05-05_拡張機能-設計メモ]] - 元の3機能設計（本プランで実装フェーズ化）
- [[templates/_フォーム入力ガイド]] - フォーム入力の導入手順
- [[README]] - スキーマ・命名規則 SSoT
- [[_index]] - training-log トップ
